server:
  port: 9999

#logging:
#  level:
#    org.springframework.security: trace

spring:
 application.name: auth-server

spring.security:
 user:
  name: user1
  password: password
  oauth2:
   authorizationserver:
    client:
     messaging-client:
      registration:
      client-id: "messaging-client"
      client-secret: "{noop}secret"
      client-authentication-methods:
       - "client_secret_basic"
      authorization-grant-types:
       - "authorization_code"
       - "refresh_token"
       - "client_credentials"
      redirect-uris:
       - "http://127.0.0.1:8080/login/oauth2/code/messaging-client-oidc"
       - "http://127.0.0.1:8080/authorized"
      post-logout-redirect-uris:
       - "http://127.0.0.1:8080/logged-out"
      scopes:
       - "openid"
       - "profile"
       - "message.read"
       - "message.write"
      require-authorization-consent: true

# Lookup this configuration in the common config: application.yml
# app:
#  eureka-username: ${CONFIG_SERVER_USR}
#  eureka-password: ${CONFIG_SERVER_PWD}
#  eureka-server: localhost

# eureka:
#  client:
#   service-url:
#    defaultZone: "http://${CONFIG_SERVER_USR}:${CONFIG_SERVER_PWD}@${app.eureka-server}:8761/eureka/"
#    initialInstanceInfoReplicationIntervalSeconds: 5
#    registryFetchIntervalSeconds: 5
  
#  instance:
#   leaseRenewalIntervalInSeconds: 5
#   leaseExpirationDurationInSeconds: 5
#   metadata-map:
#    username: ${CONFIG_SERVER_USR}
#    password: ${CONFIG_SERVER_PWD}

management.endpoint.health.show-details: "ALWAYS"
management.endpoints.web.exposure.include: "*"

logging:
  level:
    root: INFO
